# 个人招聘人才库与人岗匹配系统设计方案（整合版）

版本：1.1（整合设计补充与部署运维要求）  
设计日期：2026-08-28  
适用对象：单人、私有使用的招聘顾问人才库与 BD 工作台，单人使用，无需设计登录注册这些区分租户的功能，根据不同的api调用文档，封装错误码

硅基流动：bge-m3，BAAI/bge-reranker-v2-m3，最高 RPM 为 2,000；最高TPM 为 500,000，可免费；最高 RPM 为 2,000；最高TPM 为 1,000,000，**<span style="color:rgb(154, 56, 215)">0.070</span>**

<span style="color:rgb(133, 133, 133)">/ M Tokens；</span>

deepseekapi格式返回封装

| 400 - 格式错误     | **原因**：请求体格式错误  
**解决方法**：请根据错误信息提示修改请求体                                                                                 |
| -------------- | ------------------------------------------------------------------------------------------------------------------------ |
| 401 - 认证失败     | **原因**：API key 错误，认证失败  
**解决方法**：请检查您的 API key 是否正确，如没有 API key，请先 [创建 API key](https://platform.deepseek.com/api_keys) |
| 402 - 余额不足     | **原因**：账号余额不足  
**解决方法**：请确认账户余额，并前往 [充值](https://platform.deepseek.com//top_up) 页面进行充值                                  |
| 422 - 参数错误     | **原因**：请求体参数错误  
**解决方法**：请根据错误信息提示修改相关参数                                                                                |
| 429 - 请求速率达到上限 | **原因**：请求速率（TPM 或 RPM）达到上限  
**解决方法**：请合理规划您的请求速率。                                                                       |
| 500 - 服务器故障    | **原因**：服务器内部故障  
**解决方法**：请等待后重试。若问题一直存在，请联系我们解决                                                                         |
| 503 - 服务器繁忙    | **原因**：服务器负载过高  
**解决方法**：请稍后重试您的请求                                                                                      |

---

## 1. 目标、范围与已确认约束

### 1.1 总目标

建立一个以人才库为中心的招聘工作系统，覆盖：

1. 简历主动/被动入库、原件存储、解析、去重、版本管理和 Excel 总表；
2. JD 上传、解析、维护和关闭；
3. 以关键词、JD、批量 JD 为入口的人岗匹配与 Excel 导出；
4. 推荐、面试漏斗、项目数据看板；
5. 公司 Mapping 的树形维护、报告和 Excel/PDF 导出；
6. 基于普通网页搜索的 BD 线索助手。

第一优先级为：在 10 万份当前有效简历规模下，检索首屏稳定秒级响应，同时不牺牲硬条件准确性和候选人排序质量。

### 1.2 已确认约束

| 项目    | 设计取值                                                                                                                                                    |
| ----- | ------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 使用形态  | 单人本地/私有部署，不做多租户、角色权限、产品级脱敏或隐私产品能力；但本地存储层需做敏感字段加密与日志脱敏（属本地安全加固，见 12.6）                                                                                   |
| 部署形态  | Windows / macOS 电脑上独立安装使用；直接修改每一台电脑上的配置文件，不依赖网络存储                                                                                                       |
| 简历规模  | 初期按 10 万名候选人、每人 2–4 个检索块设计                                                                                                                              |
| 支持文件  | PDF、DOC、DOCX；扫描 PDF 进入 OCR 流程                                                                                                                           |
| 学校排名  | 统一采用 QS 2026 版；后续可整体切换排名版本，不重命名历史原件                                                                                                                     |
| 首次搜索  | 数据完成首次导入与索引后，首个真实检索请求也必须秒级；不允许模型加载、建索引或冷启动占用用户查询时间                                                                                                      |
| 检索目标  | 候选人检索首屏 P95 不高于 1.5 秒，P99 不高于 2.5 秒；长篇推荐理由不计入首屏                                                                                                         |
| 原件存储  | 本机本地磁盘目录（推荐 MinIO 提供稳定对象路径和版本管理）；物理路径采用两级哈希分片，入库月份为逻辑分区（见 5.3.1）；不默认使用网络映射盘、NAS 或云同步文件夹                                                                 |
| 主技术路线 | PostgreSQL 为事实库；OpenSearch 2.x 作为全文+向量混合检索库；Redis 队列                                                                                                    |
| 删除策略  | 所有删除为软删除（deleted_at）+ 回收站 30 天恢复；到期按设置策略提示或自动清理（见 4.6）                                                                                                  |
| 维护策略  | 每日 forcemerge / VACUUM ANALYZE / 日志滚动清理；每月 VACUUM FULL；match_result 快照保留 6 个月（见 13.1、13.2）                                                              |
| 模型服务  | 全部通过 API 调用，不本地部署：LLM 用 DeepSeek V4 Flash（默认）或 DeepSeek V4 PRO（可配置）；Embedding 用 BGE-M3；重排用 bge-reranker-v2-m3。全部 API 的地址、密钥、模型可在设置页独立填写、更换与测试（见 12.7.3） |

### 1.3 不做的事情

- 不以爬虫方式绕过 Boss、猎聘、脉脉等网站的登录、验证码或反爬机制；
- 不把大模型逐份阅读 10 万份简历作为检索方案；
- 不让 Excel 成为数据的唯一来源或回写入口；
- 不让 LLM 在主检索链路中决定硬性淘汰或最终排序。

---

## 2. 总体架构

```text
┌──────────────┐
│ 本地上传/拖拽 │────┐
└──────────────┘    │
                      ▼
┌──────────────┐  ┌─────────────────────┐
│ Agent 邮箱   │─▶│ 接入与入库编排服务   │
└──────────────┘  │ 校验/哈希/去重/任务  │
                  └──────┬───────┬──────┘
                         │       │
             原件写入    │       │ 事务写入
                         ▼       ▼
              ┌──────────────┐ ┌──────────────────┐
              │ 原件库        │ │ PostgreSQL        │
              │ 哈希分片路径   │ │ 事实、版本、状态  │
              └──────────────┘ └──────┬───────────┘
                                      │ Outbox/队列
                                      ▼
                  ┌──────────────────────────────────┐
                  │ Worker：文本提取/OCR/解析/标准化  │
                  │ 向量化/重排特征/自动匹配/导出     │
                  └─────────────┬────────────────────┘
                                ▼
              ┌──────────────────────────────────────┐
              │ OpenSearch / Elasticsearch            │
              │ BM25 + 字段过滤 + 向量 ANN + RRF      │
              └─────┬───────────────────┬────────────┘
                    ▼                   ▼
           人才检索 / 人岗匹配     Excel / 看板 / Mapping
```

### 2.1 组件职责

| 组件             | 责任                                                                       |
| -------------- | ------------------------------------------------------------------------ |
| Web 前端         | 人才表、JD 列表、匹配结果、看板、Mapping 树和 BD 报告                                       |
| API 服务         | 业务接口、查询编排、状态更新、导出任务创建                                                    |
| PostgreSQL     | 候选人、简历版本、JD、状态流、匹配快照、Mapping、导出任务的唯一事实来源                                 |
| 原件库            | PDF、Word、邮件 EML、导出的 Excel/PDF；哈希分片扁平化物理路径，入库月份为逻辑分区                      |
| Redis + Worker | 长任务队列（RQ）、OCR、解析、embedding、重排、导出、重试和预热；任务状态持久化于 PostgreSQL task 表（见 2.4） |
| 搜索引擎           | 倒排检索、结构化过滤、向量检索、融合和分页                                                    |
| 模型服务（API）      | 简历/JD 结构化解析、embedding、reranker、推荐理由生成、意图解析与提问改写                          |

### 2.2 推荐实现栈

| 层              | 推荐默认                                                         |
| -------------- | ------------------------------------------------------------ |
| 前端             | Next.js/React，表格优先，支持大字段筛选和原件预览；三栏布局（左侧筛选器、中央表格、右侧详情抽屉）      |
| 后端             | Python FastAPI                                               |
| 事实库            | PostgreSQL 16                                                |
| 原件库            | 本机 MinIO；也可先用受控本地目录                                          |
| 队列             | Redis + RQ（Celery 为备选）；任务状态持久化于 PostgreSQL（见 2.4）            |
| 文档处理           | PDF 文本提取、DOC/DOCX 转换、PaddleOCR 扫描件识别                         |
| 检索             | OpenSearch 作为首选；也可替换为 Elasticsearch                          |
| LLM（API）       | DeepSeek V4 Flash（默认）或 DeepSeek V4 PRO（可配置）；仅用于解析、意图改写和证据化理由 |
| Embedding（API） | BGE-M3                                                       |
| 重排（API）        | bge-reranker-v2-m3                                           |
| 模型调用方式         | 全部调用 API，不本地部署模型；不依赖本机 GPU                                   |

本方案冻结 OpenSearch 2.x 为首版统一搜索引擎：它同时保存检索投影、BM25 倒排、结构化过滤字段和向量。10 万规模没有必要初期同时维护专用向量库和全文检索库；如后续替换为 Elasticsearch，需重新跑完整回归集和压测。

### 2.3 查询意图解析（检索入口统一抽象）

无论用户从全局搜索框、关键词、自然语言还是 JD 进入，统一先经过一次 AI 意图解析（LLM 函数调用），把输入拆成三条通道，避免把过滤条件错误地塞进向量检索：

```text
用户输入（如："5年经验 985硕 Java" 或自然语言）
│
▼
【AI 意图解析】（LLM 函数调用）
│
├── 数值/枚举（年限、学历、地点、QS、状态） ──► OpenSearch Filter（硬性拦截，毫秒级）
│
├── 实体词（Java、金融、公司名、技术栈）    ──► BM25 全文检索（命中关键词）
│
└── 剩余语义                                ──► 向量 ANN（泛化匹配）
```

- 公司名也走关键词检索（BM25）；
- 姓名、行业、电话、学历等可枚举/数值信息解析为过滤条件，不进入语义检索；
- 硬条件必须在 BM25 和向量检索之前共同过滤，不能先取向量 Top K 再过滤，否则小结果集和强条件下会产生漏召回。

### 2.4 异步任务队列设计（Redis RQ + 任务状态持久化 + 前端轮询）

#### 2.4.1 问题背景

模型能力全部走 API 调用（DeepSeek V4、BGE-M3、bge-reranker-v2-m3），单次 API 调用延迟约 1–3 秒。如果把解析、向量化、重排、批量匹配、Excel 导出、全量重算等操作同步执行，用户界面会被阻塞数秒甚至数分钟。设计目标是：

- 所有耗时操作（>500ms）一律异步化，接口立即返回任务 ID，不阻塞用户操作；
- 检索关键路径（第 7 章主链路）不依赖上述异步任务，只依赖已就绪的索引与缓存；
- 任务状态持久化在 PostgreSQL，进程重启/机器重启后任务不丢失、可恢复；
- 前端通过轮询展示进度，界面左上角统一显示任务中心。

#### 2.4.2 技术选型与整体流程

| 组件     | 选型                                  | 职责                                       |
| ------ | ----------------------------------- | ---------------------------------------- |
| 队列     | Redis（RQ 或 Celery，推荐 RQ：单机轻量、无额外依赖） | 任务瞬时分发与 Worker 消费；只做"待执行任务"的排队，不承担状态持久化  |
| 任务状态库  | PostgreSQL task 表                   | 任务生命周期（状态、进度、入参快照、结果、错误、重试次数）的**唯一事实来源** |
| Worker | 独立进程（常驻，启动时预热）                      | 消费队列、调用模型 API、写结果、推进任务状态                 |
| 前端     | 轮询接口（默认 1s 间隔，任务中心页 3s）             | 拉取任务状态与进度，展示进度条与失败原因                     |

```text
API 请求（上传/匹配/导出/重算）
  │ 校验入参、生成 task_id（幂等键）
  ▼
① 写 PostgreSQL task 表：PENDING（入参快照、优先级、重试策略）
② 推送 Redis 队列：{queue_name, task_id}
  ▼
③ RQ Worker 消费（按优先级队列分派，多进程并发）
  ├─ 调用模型 API / 执行任务
  ├─ 更新 task 表：RUNNING → SUCCESS / FAILED / RETRY（指数退避）/ DEAD_LETTER
  └─ 结果写回对应业务表（resume_revision、match_result、导出文件等）
  ▼
④ 前端轮询 GET /api/tasks/{task_id}
  └─ 展示状态、进度、错误码；失败可"重试"，完成任务可"查看结果"
```

#### 2.4.3 队列划分与优先级

| 队列         | 任务类型                             | 优先级 | 并发                  |
| ---------- | -------------------------------- | --- | ------------------- |
| `priority` | 用户手动触发的单文件解析、单 JD 匹配、立即导出        | 高   | 2–4 Worker          |
| `normal`   | 批量导入解析、批量匹配、自动反向匹配               | 中   | 按 CPU 核数 - 1        |
| `batch`    | 首次全量导入、全量重算（更换 Embedding 后）、索引重建 | 低   | 2–4 Worker，限速调用 API |
| `export`   | Excel/PDF 导出、Mapping 报告生成        | 中   | 2 Worker            |
| `dead`     | 多次重试仍失败的任务                       | 不执行 | 人工处理/定时重试           |

- 高优先级队列使用 Redis 优先级队列（`priority` + score），确保用户手动操作不被后台批量任务饿死；
- 首次全量导入（10 万份）走 `batch` 队列，通过"任务批次"（batch_id）管理，可暂停/继续/断点续传；
- 所有队列共用同一套 Worker 进程，按队列权重分配并发配额。

#### 2.4.4 任务状态机与持久化

```text
PENDING ──► QUEUED ──► RUNNING ──► SUCCESS
              │           │
              │           ├──► FAILED ──►（手动重试/自动重试）──► QUEUED
              │           │
              │           └──► RETRY_WAIT（指数退避 1s/5s/30s/2m/10m，最多 5 次）
              │                                └──► DEAD_LETTER
              └──►（Worker 崩溃后重启）──────► 依据 task 表状态恢复 QUEUED
```

task 表核心字段：

```text
task_id、task_type、batch_id、queue_name、priority、
status（PENDING/QUEUED/RUNNING/SUCCESS/FAILED/RETRY_WAIT/DEAD_LETTER）、
progress（0–100）、入参快照 JSON、结果引用、错误码、错误消息、
attempts、max_attempts、next_retry_at、幂等键、created_at、updated_at
```

关键规则：

1. **先写库、后入队**：task 行写入 PostgreSQL 成功后才推送 Redis，保证队列消息不丢（Redis 只做瞬时缓冲）；
2. **Worker 幂等消费**：消费任务后先按 task_id 查库，若已 SUCCESS 则直接确认（防重复投递）；
3. **崩溃恢复**：Worker 启动时扫描 RUNNING/RETRY_WAIT 超时任务，超过心跳阈值（默认 5 分钟）的重新入队；任务进度由 Worker 周期性写库（大任务每 N 份/每 5s 更新一次 progress）；
4. **幂等键**：同一上传请求/重试请求依据 idempotency_key + SHA-256 复用已有任务，不重复执行（与 5.1 一致）；
5. **结果引用**：任务结果（match_run_id、导出文件 blob_id、解析 JSON 位置）以引用形式写入 task 表，不做大对象存储。

#### 2.4.5 前端轮询与任务中心

- 页面左上角常驻任务中心（进度条列表），默认每 1 秒轮询 `/api/tasks?filter=active`，任务完成后提示可查看详情；
- 任务中心页支持：按类型/状态筛选、查看进度、失败原因（错误码 + 中文提示，见 12.5）、手动重试、批量取消；
- 轮询接口做服务端聚合：只返回增量变化的任务（`updated_at > 上次拉取`），避免大列表重复传输；
- 长任务（全量重算、批量导入）在任务中心展示批次总进度（如"已处理 34,521 / 100,000，预计剩余 12 分钟"）。

#### 2.4.6 与 API 1–3 秒延迟的关系

| 场景                     | 处理方式                                                            |
| ---------------------- | --------------------------------------------------------------- |
| 简历/JD 解析（LLM API 1–3s） | 异步：接口立即返回"解析中"，前端轮询任务中心；单文件可"阻塞等待至多 2s + 轮询兜底"以兼顾体验             |
| 匹配首屏（检索主链路）            | **不调用异步 API**：只用已就绪的 BM25/向量/重排索引，秒级返回（见第 7 章）；长理由异步生成，首屏先给模板理由 |
| 批量匹配 / Excel 导出        | 异步：立即返回任务 ID，完成后前端提示下载；match_run 快照保证导出内容可追溯                    |
| JD 上传                  | 立即预计算 JD 向量并缓存（预计算异步执行），匹配时直接复用缓存，查询路径不等待 API                   |
| Embedding API 抖动       | 任务重试指数退避；查询侧已有向量缓存的 JD 继续可用，自由文本查询退化为 BM25（见第 13 章）             |

#### 2.4.7 监控与运维

- Worker 健康：`/health` 返回各队列积压量、Worker 存活数、最近心跳；
- 队列积压告警：单队列积压 > 阈值（如 normal 队列 > 5,000）时页面提示"后台处理较慢"；
- 任务重试与 DEAD_LETTER：每日自维护任务扫描 DEAD_LETTER，支持一键重新入队（见第 13 章）；
- 模型 API 限速：对同一 API 的 QPS 做客户端限流（默认按供应商配额配置），避免批量任务触发 429 导致大面积重试。

---

## 3. 首次使用与秒级检索设计

### 3.1 "首次使用"定义

首次启动后有三种状态，必须在界面上区分：

1. 空库：可立即使用检索，但结果为空；
2. 首次导入中：系统显示导入/解析/索引进度，不能承诺未索引的简历可被正确搜索；
3. 首次索引完成：此时第一条真实查询也必须秒级响应，不允许因为首次模型加载或索引预热而变慢。

也就是说，10 万份未完成解析和索引的简历不可能既立刻可检索又准确；设计承诺的是"索引完成后的首次查询无冷启动"，而不是把初始批量导入伪装成实时查询。

### 3.2 无冷启动机制（模型 API 化版）

由于模型全部走 API（不本地部署），冷启动的重点从"下载模型"转变为"API 连通性校验 + 预计算 + 预热"：

1. 安装/初始化阶段校验 LLM、Embedding、Reranker 三个 API 的连通性、密钥与可用额度；
2. 服务启动阶段创建索引 schema、分析器、技能词典和学校映射；
3. 首次数据导入在后台批量解析、批量 embedding、批量写索引（走 2.4 的 `batch` 队列，可暂停/继续/断点续传）；
4. 首次全量导入写入新物理索引；完成抽样检索、段合并稳定和预热后，再用 alias 原子切换为当前索引；
5. 服务启动时对 Embedding/Reranker API 做连通性与时延探针，不按首个用户请求触发初始化；
6. JD 上传后立即预计算 JD 结构化结果和向量；相同 JD 内容复用缓存；
7. 预热集至少覆盖精确技能、中英混合、强过滤和 JD 匹配四类查询，并覆盖全部分片、embedding 服务与 reranker 服务；
8. 只有"模型 API 就绪、索引 alias 已切换、预热集达标、当前索引版本一致、Worker 健康"同时通过，系统才进入 READY 并开放搜索；
9. 搜索缓存键为 query_normalized + filters + index_generation + JD_revision + model_version；候选人局部更新只失效受影响的详情和匹配缓存，不清空全部热点结果；查询缓存 TTL 默认 1 小时；
10. JD 上传后立即预计算向量并缓存，匹配时直接复用；同一份简历解析后向量存入 OpenSearch，不重复计算。

### 3.3 延迟预算

| 阶段                       | P95 预算 |
| ------------------------ | -----: |
| 查询理解与规则扩展（AI 意图解析）       |  60 ms |
| 查询 embedding（预计算 JD 为 0） | 120 ms |
| BM25 与向量 ANN 并行召回        | 250 ms |
| 融合、去重、字段补全               |  80 ms |
| Top 100 重排               | 400 ms |
| API 序列化、网络与 DB 读取        | 180 ms |
| 后端 API 目标                |  1.2 s |
| 浏览器首屏目标（含余量）             |  1.5 s |

详细自然语言推荐理由不放在以上关键路径中。首屏先返回模板化理由；用户点击详情或导出时，后台补充生成长理由。

### 3.4 机器基线（不用考虑机械，按大众常用mac、windows性能为计算标准）

READY 后的验收分为两项：

1. 独立重启后的第一条真实查询端到端不高于 2.5 秒；
2. 随后的代表性压测中，浏览器首屏 P95 不高于 1.5 秒、P99 不高于 2.5 秒。

---

## 4. 数据标准与核心模型

### 4.1 候选人、文件和版本必须分离

| 实体                | 作用                                                          |
| ----------------- | ----------------------------------------------------------- |
| candidate         | 稳定的人才主档案，不因文件更新而变更 ID                                       |
| resume_document   | 某位候选人的逻辑简历                                                  |
| resume_revision   | 一份不可变原件版本，关联 resume_document，记录文件、来源、解析版本和当前/历史标记           |
| blob              | 物理文件哈希（SHA-256）、哈希分片存储路径、ingest_month 逻辑分区（首次入库年月）；相同二进制可复用 |
| candidate_profile | 当前可展示、可筛选的候选人投影                                             |
| extraction_run    | 某份原件由某个解析模型/提示词得到的结果                                        |
| field_evidence    | 每个字段的值、置信度、原文片段、页码                                          |
| correction        | 手工改动记录；当前值优先于机器值                                            |
| search_profile    | 写入搜索引擎的候选人摘要、关键词、元数据、向量和经历块                                 |

当前字段优先级固定为：人工确认值 > 当前简历的解析值 > 计算字段。

### 4.2 关键表及字段

| 表                       | 核心字段                                                                                                                                                                                   |
| ----------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| candidate               | candidate_id、姓名、手机号、身份证号、出生日期、所在地、候选人状态、创建时间、deleted_at（软删除时间，见 4.6）                                                                                                                   |
| candidate_profile       | recent_industry、longest_tenure_industry、技术方向、业务方向、总年限、最高学历、毕业年份、学校、学校等级、QS2026 排名、核心经历摘要（工作年限：{总年限}年。技术方向：{标准词、标准词、……}。业务方向：{标准词、标准词、……}。项目经验：{项目1}（{技术栈}），{业务场景}；{项目2}（{技术栈}），{业务场景}） |
|                         |                                                                                                                                                                                        |
|                         |                                                                                                                                                                                        |
|                         |                                                                                                                                                                                        |
| resume_document         | resume_document_id、candidate_id、语言、用途、是否当前逻辑简历、创建时间、deleted_at                                                                                                                         |
| resume_revision         | revision_id、resume_document_id、revision_no、原文件名、展示名、MD5、SHA-256、来源、blob_id、是否当前版本、入库时间、解析状态、deleted_at                                                                                 |
| job                     | job_id、公司、岗位稳定标识、状态、优先级、deleted_at；不保存随版本变化的解析字段                                                                                                                                       |
| job_revision            | job_revision_id、job_id、原件/文本、解析 JSON、AI 分类、硬条件、版本号、是否当前                                                                                                                                |
| candidate_job_case      | case_id、candidate_id、job_id、当前处理状态、首次推荐时间、最后活动时间、deleted_at；稳定承载招聘流程                                                                                                                   |
| match_run               | match_run_id、触发方式、查询/JD 版本、索引版本、模型版本、创建时间；不可变检索快照                                                                                                                                      |
| match_result            | match_result_id、match_run_id、candidate_id、case_id、简历版本、JD 版本、分项得分、总分、理由、处理状态                                                                                                           |
| application_stage_event | case_id、阶段、发生时间、原因码、备注；不绑定一次算法结果                                                                                                                                                       |
| company                 | 公司、赛道、调研负责人、调研状态、调研总结、deleted_at                                                                                                                                                       |
| org_snapshot            | 公司架构版本、生成时间、说明                                                                                                                                                                         |
| org_node                | snapshot_id、节点类型、名称、父节点、人数、HC、备注、排序、deleted_at                                                                                                                                         |
| mapping_person          | org_node_id、候选人关联、姓名、岗位、汇报人、年限、技能、薪资、人才等级、跳槽意向、风险标签、deleted_at                                                                                                                         |
| bd_lead                 | 搜索任务、关键词、公司、岗位、来源链接、发现时间、置信度、人工核验状态、deleted_at                                                                                                                                         |
| correction_logs         | correction_log_id、table_name、row_id、字段名、旧值、新值、修改人、修改时间、回滚标记；支持逐条回滚（见 12.5.6）                                                                                                           |

### 4.3 基础枚举

候选人状态：待核验、可用、暂不考虑、归档。  
JD 状态：草稿、开放、暂停、已招满、取消、归档。  
候选人-JD 流程：待评估、待联系、已联系、有意向、已推荐、初试、复试、终试、Offer、入职、客户拒绝、候选人拒绝、暂缓、岗位关闭。  
匹配处理：未处理、保留、短名单、排除。  
Mapping 调研状态：待调研、调研中、待核验、已完成、已失效。

### 4.4 关键口径

1. 当前简历：每个 candidate 可有中英文、针对不同方向、作品集等多个 resume_document；默认检索使用用户标记为"主简历"的当前 revision，可在详情切换。
2. QS：采用 QS World University Rankings 2026 世界大学综合排名，不使用学科排名。院校别名、并列名次、分校归属维护在 school_reference 表；国内学校等级优先级为 9 > 2 > 双 > 普，海外学校等级为海。QSNA 仅表示无对应排名，不自动视为学历不合格。
3. 硬条件：只对人工确认或字段置信度达到阈值的 canonical 字段执行排除。缺失、低置信度、日期冲突的字段不自动淘汰，返回"待确认"。相关年限在入库时按技术/业务 taxonomy 生成 experience_years_by_taxonomy 映射；检索时以当前 JD 的 taxonomy 取交集并计算，不使用单一静态"相关年限"字段。

### 4.5 简历解析的标准化输出模板

简历经 AI 结构化后，按以下固定模板落库，保证检索字段的一致性：

```text
姓名：{姓名}
行业（两个口径，分别取最近一份工作与任职年限最高工作的行业，如互联网、金融）
技术方向：{标准词、标准词、……}
业务方向：{标准词、标准词、……}
年龄：{由明确出生日期计算，如果没有按毕业年份推断，}
毕业时间与学校等级：如 "21-9硕+qs排行"（{毕业年份}-{等级}{学历}+{qs前50、前100、前150、前200、前300，前300之后}）
电话：{电话}
所在地 / 期望地：所在地-{城市} / 期望地-{城市}

教育背景：{毕业年份}年，{学历}，{学校等级映射：9（985）、2（211）、双（双一流）、普（普通）、海（海外）}，QS排名{前五十/前一百/前一百五/前两百/前三百}。
工作年限：{总年限}年。
技术方向：{标准词、标准词、……}。业务方向：{标准词、标准词、……}。
工作履历：{岗位1} at {公司1}（{行业}），{职责摘要}；{岗位2} at {公司2}（{行业}），{职责摘要}。
项目经验：{项目1}（{技术栈}），{业务场景}；{项目2}（{技术栈}），{业务场景}。
```

简历向量化的关键约束：向量内容只承载**硬技能、行业场景、具体技术栈、业务指标**相关的关键事实；团队协作、沟通能力、抗压能力、领导力、学习能力强等通用软素质词汇**不得进入向量与匹配依据**。匹配动作输出的核心职责一律采用"动词 + 宾语 + 指标"的紧凑短语。

### 4.6 软删除与回收站

**问题背景**：用户可能误删一个候选人，或误关一个岗位。如果删除操作直接物理移除数据，数据将永久丢失且无法恢复。

**设计原则**：所有删除操作一律为**软删除**（标记 `deleted_at`，不物理移除数据），配合"回收站"提供 30 天恢复窗口；超过 30 天自动物理清理（或提示用户确认）。

#### 4.6.1 软删除范围

| 对象                   | 软删除方式                                                                             | 界面入口              |
| -------------------- | --------------------------------------------------------------------------------- | ----------------- |
| 候选人 candidate        | 置 `deleted_at`；详情、检索、匹配、看板默认排除已删除项，但保留其历史关系（case、stage_event、match_result 不被级联删除） | 候选人详情"删除"按钮（二次确认） |
| 简历版本 resume_revision | 置 `deleted_at`；原件 blob 不删除（可能被其他 revision 引用）                                     | 简历版本列表"删除"        |
| 岗位 job               | 置 `deleted_at`；历史匹配快照、看板数据保留                                                      | JD 详情"关闭/删除"      |
| 候选人-JD 流程 case       | 置 `deleted_at`；看板统计保留（按当时快照口径）                                                    | 匹配结果页"移除"         |
| 公司/组织节点 Mapping      | 置 `deleted_at`；快照不受影响                                                             | Mapping 树节点删除     |
| BD 线索 bd_lead        | 置 `deleted_at`                                                                    | BD 列表"删除"         |

关键规则：

1. **级联策略**：软删除父对象不级联删除子对象，只取消关联展示；子对象在回收站中随父对象一并恢复；
2. **检索排除**：所有检索、匹配、看板、Mapping 查询统一带 `deleted_at IS NULL` 条件；恢复后立即重新可见；
3. **原件 blob**：软删除不触碰物理文件（哈希分片路径不变）；仅当某个 blob 不再被任何 revision 引用且其候选人在回收站中彻底清理时才物理删除；
4. **操作留痕**：删除/恢复动作写入 `correction_logs`（见 12.5.6），可从回收站查看"谁删的、何时删的"。

#### 4.6.2 回收站

- 导航入口：左侧导航"回收站"页面，按类型（候选人/JD/简历/流程/BD 等）分类列出软删除对象，展示删除时间与原因；
- 支持：单条恢复、批量恢复、彻底删除（物理清理，需二次确认并提示不可恢复）；
- 恢复动作：清除 `deleted_at`，级联恢复其依赖对象（如候选人下被软删的 resume_document、case），并触发该对象重新进入检索索引；
- 彻底删除：仅对回收站中对象执行，需用户确认；删除后同步清理其 blob 引用（无引用时物理删除原件）与检索索引文档。

#### 4.6.3 自动清理策略

- 回收站对象保留 **30 天**；到期后进入"待清理"列表；
- 默认行为：每日自维护任务扫描到期对象并**提示用户确认清理**（避免误删无法恢复）；可在设置页（12.7.6）选择"到期自动清理"或"到期仅提醒"；
- 自动清理执行时：物理删除数据库记录 → 清理 blob 引用（引用计数为 0 时物理删除文件）→ 从检索索引删除文档 → 写清理日志。

---

## 5. 简历管理模块

### 5.1 主动入库

支持单文件上传、批量上传和文件夹拖拽。接收 PDF、DOC、DOCX，不接收扫描件、图片，前端先显示任务队列和每个文件状态。每个文件对应一个异步任务（见 2.4），上传接口立即返回，解析进度由任务中心轮询展示。

入库状态机：

```text
RECEIVED
  → VALIDATING
  → STORED
  → PARSING
      ├─ 正常解析 → INDEXING → READY
      └─ 低置信度/疑似重复 → NEEDS_REVIEW
          ├─ 确认可用 → INDEXING → READY
          ├─ 修改字段/重跑 → PARSING
          └─ 确认无效 → ARCHIVED

异常分支：REJECTED / RETRY_WAIT / DEAD_LETTER
```

具体规则：

1. 上传时流式计算 MD5、SHA-256 和文件大小；
2. MD5 相同只作为快速命中；SHA-256 相同视为同一原件，不重复写物理文件，新的提交记录仅引用已有 blob；
3. 电话、邮箱、姓名相同自动关联至现有候选人，并更新该候选人现有数据；
4. 新候选人创建 candidate 和 revision 1；
5. 同一候选人的新简历创建新 revision，不覆盖旧原件；
6. 旧当前版改为历史版，最近一版历史简历至少保留；系统默认保留最近一版版本；
7. 当前版本解析成功后，更新 candidate_profile、search_profile、Excel 数据视图和自动匹配任务；
8. 同一上传请求重试时，依据 idempotency_key + SHA-256 返回已有任务，不重复入库；
9. 解析失败 → 标记 NEEDS_REVIEW，保留原文，可手动重试，不阻塞后续文件；
10. 索引写入失败：Revision 保持 PARSED 状态，Worker 指数退避重试（最多 5 次），未 READY 前不参与检索。

### 5.2 被动入库：Agent 邮箱

使用 IMAP，优先启用 IDLE 实时监听，不支持时每 1–5 分钟轮询一次。

邮件游标不使用"上次关闭时间"作为主依据，而使用：

```text
mailbox_id + UIDVALIDITY + UID + attachment_index
```

每个附件独立进入入库队列。只消费发件人域名匹配 boss.com、liepin.com 的邮件，可在设置页维护白名单。

扫描器在"邮件/附件处理台账已持久化"后即可推进邮箱扫描水位，而不是等待每个附件最终成功。失败附件由独立重试和 DEAD_LETTER 处理；借助 mailbox_id + UIDVALIDITY + UID + attachment_index 幂等键，既不会让一个毒附件阻塞后续邮件，也不会漏入或重复入库。邮件监听断线后，重连从上次 UIDVALIDITY/UID 继续拉取，不漏不重。

### 5.3 文件命名

统一采用：

```text
姓名-毕业年份+学校等级学历-QS排名-技术方向-业务方向.pdf
```

示例：

```text
张三-19+9硕-QS45-Java-金融.pdf
李四-21+海硕-QS120-大模型算法-智能汽车.pdf
```

规则：

1. 毕业年份取最高全日制学历的毕业年份；同等学历取最近毕业年份；
2. 学校等级优先级：985 = 9，211 = 2，双一流 = 双，普通 = 普，海外 = 海；
3. QS 使用 QS World University Rankings 2026 世界大学综合排名；同名校区和别名按 school_reference 统一；无排名写 QSNA；
4. 学历使用 博、硕、本、专；
5. 技术与业务方向来自来自ai解析之后得出，举例：技术：Java、Java+agent、python、数据工程......，业务：金融风控、电商风控、金融交易、财务管理、物流履约........；
6. 文件系统真实路径使用 SHA-256 哈希分片（见下方"原件目录设计"），避免同名候选人覆盖和相同文件重复写入。

#### 5.3.1 原件目录设计：哈希分片 + 月份逻辑分区

**问题背景**：若按"入库月份"分物理目录（`2026-08/`、`2026-09/`），持续使用场景合理（每月几千份）；但首次批量导入时几万份简历同一天入库，会全部堆进同一个月份目录，导致：单目录数万个文件，文件系统遍历变慢（Windows 尤甚）；备份/恢复时单目录文件过多导致性能下降；与"按月归档"的设计初衷背离。

**解决方案**：物理路径改为**两级哈希分片**，入库月份只作为**逻辑分区**记录在数据库，不再作为物理目录。

```text
blobs/
  {sha256[0:2]}/
    {sha256[2:4]}/
      {sha256}.pdf        ← 物理文件名 = 完整 SHA-256，不含中文与展示信息
```

设计要点：

1. **分片桶**：取 SHA-256 前 4 个字符，前 2 位为一级目录、第 3–4 位为二级目录，共 256 × 256 = 65,536 个桶。10 万份原件平均每桶约 1–2 个文件，目录遍历、备份、恢复均为常量级开销，Windows 文件系统无压力；
2. **物理文件名 = 纯哈希**：`{sha256}.pdf`，不含中文、空格与展示信息，规避 Windows 长路径（260 字符限制）与中文文件名兼容性问题；展示名、原文件名、旧版本标记全部保存在 resume_revision 表，由应用层渲染；
3. **月份为逻辑分区**：blob 表记录 `ingest_month`（首次入库年月）字段，用于按月统计、归档、清理与导出；需要按月份物理归档时，通过数据库查询定位哈希路径后执行复制/移动，而不是直接按目录扫描；
4. **去重**：同一 SHA-256 再次出现时只引用已有 blob，不复制文件、不新增物理目录；
5. **不迁移**：物理 blob 一旦写入即不移动、不改名；新月份不产生新目录，避免版本更新破坏引用或重复写入；
6. **备份/恢复**：按桶并行遍历复制，避免单目录遍历瓶颈；恢复时逐桶校验 SHA-256 与数据库一致性。

对比：

| 方案                | 单目录文件数（10 万份） | 目录遍历          | 备份恢复    | 月度归档                 |
| ----------------- | ------------- | ------------- | ------- | -------------------- |
| 按月分目录             | 数万/目录         | 慢（Windows 明显） | 性能下降    | 天然按月                 |
| 哈希分片 + 月份逻辑分区（采用） | 1–2/桶         | O(1) 常量级      | 按桶并行，稳定 | 数据库按 ingest_month 定位 |

### 5.4 人才信息核心视图

人才库列表支持筛选、全文搜索、保存搜索条件、原件预览和 Excel 下载。

固定展示字段：

1. 姓名；
2. 最近一份工作的行业；
3. 任职时间最长工作的行业；
4. 技术方向、业务方向，举例：技术：Java、Java+agent、python、数据工程......，业务：金融风控、电商风控、金融交易、财务管理、物流履约........，技术与业务，最多各自例举两个；
5. 年龄；
6. 是否有AI背景
7. 毕业时间 + 学校等级 + 学历 + QS2026；
8. 电话；
9. 所在地，简历没有就根据最近一份工作的公司所在地推测；
10. 工作经历核心提取，格式：（工作年限：{总年限}年。  
    工作履历：{岗位1} at {公司1}（{行业}），{职责摘要}；{岗位2} at {公司2}（{行业}），{职责摘要}。  
    项目经验：{项目1}（{技术栈}），{业务场景}；{项目2}（{技术栈}），{业务场景}）；
11. 当前简历链接，预览或者下载；
12. 最近更新时间、候选人状态（绑定已推荐岗位）、人工备注。

"工作经历核心提取"采用固定结构：最近职位、最长任职职位、关键技术/业务场景、可量化成果、可迁移能力、待确认信息。每个结果可直接人工修改。

### 5.5 入库后的自动匹配

当简历状态变为 READY，系统异步用"候选人 → 开放岗位"的反向匹配流程检索所有开放 JD，生成 Top N 潜在岗位。该任务不阻塞简历入库；结果写入 match_run/match_result 并在候选人详情展示。只有用户进入业务推进时才创建 candidate_job_case。

---

## 6. JD 信息管理模块

### 6.1 输入与解析

支持：

- 直接粘贴文本；
- 单个/批量 Word；
- 单个/批量 Excel；
- Excel 中每行一个 JD，配置岗位名称、公司、JD 文本三列映射。

JD 文件与简历同样保留 revision。新版本覆盖"当前展示版本"，旧版本 JD 删除。

JD 解析标准化模板（落库结构）：

```text
岗位：{岗位名称}
岗位要求：{学历}学历，QS排名{等级}。
经验要求：{最低年限}年。
技术方向：{标准词}。业务方向：{标准词}。
核心职责：{动词+宾语+指标}；{职责2}。
必需技能：{技能1}、{技能2}、{技能3}。
加分场景：{行业背景}、{项目类型}。
```

实时计算类岗位允许只走规则 + 关键词检索，不必依赖完整语义链路。

### 6.2 JD 结构化字段

| 类别 | 字段                        |
| -- | ------------------------- |
| 基础 | 岗位名称、公司、部门、地点、薪资、优先级、截止日期 |
| 分类 | AI/非AI、技术方向、业务方向、行业、职级    |
| 必备 | 必备技能、最低年限、学历、行业、语言、地点、证书  |
| 加分 | 优先技能、优先公司/项目背景、加分行业       |
| 排除 | 不接受城市、明显不符的方向、其他硬性排除项     |
| 展示 | 岗位最核心要求、完整 JD 原文、人工校准结果   |

AI/非AI 采用三态内部值：核心 AI、AI 相关、非 AI。对外列表按产品需要展示 AI 或非 AI；AI 相关默认标记待人工确认。

### 6.3 页面与状态

列表页只展示：岗位名称、公司名称、AI/非AI、岗位最核心要求。  
详情页展示完整 JD、解析字段、版本历史、关联候选人与推进漏斗。  
关闭岗位不是删除，状态改为已招满、取消或归档；历史匹配和看板不受影响。

---

## 7. 人岗匹配模块

### 7.1 触发入口

1. 输入核心关键词；
2. 输入带筛选条件的自然语言；
3. 上传单个 JD；
4. 上传多个 JD 文件；
5. 简历入库后自动匹配所有开放岗位；
6. JD 开放/更新后自动匹配人才库。

所有入口先经过统一的 AI 意图解析（见 2.3）：数值/枚举进 Filter、实体词进 BM25、剩余语义进向量 ANN。提问改写规则为：

```text
提问改写：
将姓名、行业、电话、学历之类的信息解析为关键词匹配/过滤条件；
其他内容改写为：
技术方向：{标准词}。业务方向：{标准词}。
核心职责：{动词+宾语+指标}；{职责2}。
必需技能：{技能1}、{技能2}、{技能3}。
加分场景：{行业背景}、{项目类型}。
```

### 7.2 检索与排序主链路

```text
关键词/JD
  → 查询解析：硬条件、技能别名、同义词、中英映射（AI 意图解析）
  → 硬过滤：状态、地点、年限、学历、行业、排除条件
  → 并行召回
       ├─ BM25：岗位、技能、证书、公司、项目、精确术语
       └─ Dense ANN：候选人摘要与经历块的语义召回
  → RRF 融合并按 candidate_id 去重
  → Top 50–100 cross-encoder 重排（可选重排数量，200以内）
  → 规则特征修正：必备技能覆盖、相关年限、行业、近两段经历
  → Top 20 返回（可选返回数量，200以内）
  → 模板理由立即返回；长理由异步生成
```

硬条件必须在 BM25 和向量检索之前共同过滤，不能先取向量 Top K 再过滤。否则小结果集和强条件下会产生漏召回。

首版物理索引配置冻结如下，Phase 0 压测后才允许调参：

| 项                    | 初始配置                                                                            |
| -------------------- | ------------------------------------------------------------------------------- |
| 搜索引擎                 | OpenSearch 2.x，2 个主分片，单机 0 副本                                                   |
| candidate_profile 索引 | 10 万条；BM25、过滤字段、1 个 1024 维候选人摘要向量                                               |
| experience_chunk 索引  | 20–40 万条；每块 1024 维向量，仅用于语义补充证据                                                  |
| 展示单位                 | candidate_id；块召回后按 candidate_id 聚合，保留得分最高块                                      |
| 每路召回                 | BM25 Top 300，dense Top 300                                                      |
| ANN 初始参数             | HNSW M=16、ef_construction=200、ef_search=128、num_candidates=1000/分片              |
| 强过滤                  | 提高 num_candidates 至 3000/分片；如果过滤后候选集合很小，改用精确集合检索                                |
| 过滤字段                 | 候选人状态、地点、总年限、experience_years_by_taxonomy、学历、学校等级、QS、行业、技术方向、业务方向、语言、更新时间       |
| 重排输入                 | JD 结构化摘要 + candidate search_profile + 最相关经历块；最多 768 token，超长截断，batch size 由压测冻结 |

任何模型、向量维度、chunk 上限、HNSW 参数、Top K、分片数或 GPU batch 的变化，都属于检索版本变化，必须重新通过第 15 章回归和压测。

### 7.3 匹配评分

总分不是黑盒单一分数，至少记录：

| 分项      | 作用                           |
| ------- | ---------------------------- |
| 硬条件结果   | 不满足则标记硬淘汰，而非用低分掩盖            |
| 技术技能覆盖  | 必备、加分、技能熟练度、同义词命中            |
| 相关经历    | 相似岗位、项目、职责、最近两段经历            |
| 年限      | 总年限、相关年限、管理年限                |
| 行业/业务方向 | 最近行业、最长任职行业、业务相似度            |
| 教育背景    | 学历、学校等级、QS2026               |
| 语义相关性   | JD 与 search_profile/经历块向量相似度 |
| 重排相关性   | cross-encoder 的 JD-候选人整体相关性  |

权重从默认配置起步，后续由"保留/短名单/排除/面试结果"反馈校准。年龄、姓名、照片不参与默认排序。

### 7.4 结果页与 Excel

单个岗位的结果页按总分降序显示：

1. 姓名；
2. 技术与业务方向；
3. 毕业时间与学历；
4. 电话；
5. 核心信息；
6. 推荐理由；
7. 原始简历链接；
8. 分项得分与待确认缺口；
9. 人工处理状态。

每次检索先创建不可变 match_run，并写入 match_result。批量 JD 导出采用一个 Excel 文件、每个岗位一个 Sheet；每个 Sheet 对应固定 match_run 快照，保证再次打开时能追溯当时的 JD、简历版本、分数和理由，而不是重新实时检索。

当用户对某人执行"保留、短名单、联系、推荐"等业务动作时，系统创建或复用稳定的 candidate_job_case。后续初试、复试、Offer 等 stage_event 一律绑定 case_id，而不绑定某个 match_result。这样即使简历或 JD 更新并生成新的匹配结果，历史招聘流程和看板也不会断裂或重复。

---

## 8. 数据分析与项目看板（手动标记驱动）

### 8.1 数据口径

看板只以候选人与岗位的关系记录为基础，不能以候选人全局状态统计。招聘流程由顾问手动标记驱动，每推进一次就填写备注并更新数据表，算法结果只负责初始推荐，不自动改写流程状态。

推荐量：candidate_job_case 首次进入"已推荐"的时间。  
阶段转化：绑定 candidate_job_case 的 application_stage_event 进入、通过和淘汰事件。  
岗位关闭：JD 状态变更事件。  
面试漏斗：同一岗位内的候选人去重后按阶段流转统计。

推进数据表字段（每次推进即更新）：

```text
岗位、候选人、推进次数（简历筛过为 0）、流程进度（推进/淘汰/offer/入职）、
offer 标记 0/1、入职标记 0/1、备注
```

### 8.2 页面指标

| 看板     | 指标                                 |
| ------ | ---------------------------------- |
| 推荐统计   | 按岗位、公司、周、月、季度维度统计"推荐简历总数"          |
| 岗位漏斗   | 按岗位维度统计初试、复试、终试的通过率；在候选人处更新，此处随之变动 |
| 顾问工作量  | 导入量、联系量、推荐量、短名单量、岗位关闭量             |
| 人才库健康度 | 新增量、重复率、待校验数、解析失败数、近 30 天更新量       |
| 图表     | 折线图看趋势、柱状图做岗位/公司比较、饼图看阶段占比         |

可视化实现：后端生成图表图片 + Excel 数据透视 + PPT 一键生成。不固定的"面试阶段"枚举值（按照推进、淘汰、offer、入职、拒接，动态计算，以推进次数计算该岗位的每一轮的通过率，简历筛时推进次数为0），由顾问自己编辑每一轮面试是业务、技术等等，系统不预设看板。

状态允许补录和回退，但每一次更改都写 event；报表支持"按当前状态"与"按当时快照"两种口径。

### 8.3 定时提醒（计划功能）

系统开启运行时，每天通过邮箱自动提醒两次（agent邮箱发送到顾问个人邮箱）：

- 早上 9:00：追反馈提醒——顾问给出模板，由 AI 按追反馈格式编辑后再发送，追反馈格式为微信短信文本，由顾问根据公司或者岗位预设；
- 下午 17:00：系统匹配人才未处理提醒——提醒格式为"姓名 - 岗位名"（提醒缓存次日清除）。

提醒的开关、时间、模板与发送邮箱账号均在设置页（12.7.5）中维护。

---

## 9. Mapping 报告模块

### 9.1 数据结构

公司维度：公司、赛道、调研负责人、调研状态、调研小结。  
部门节点：部门名称、父级部门、团队人数、是否有 HC、业务备注。  
人员节点：姓名、岗位、汇报人、年限、技能、预估薪资、人才等级 A/B/C、跳槽意向、风险标签、可选候选人库关联（可关联系统候选人库，也可手动录入外部在职人员）。

每次调研形成 org_snapshot。树形拖拽改变的是新快照或当前草稿快照，不直接覆写已交付报告。

### 9.2 页面能力

1. 树形编辑器：部门/人员拖拽、排序、节点新增、删除、移动；拖拽调整部门/人员层级，维护汇报关系，记录修改日志；
2. 节点侧栏：维护字段和调研备注；
3. 修改日志：记录节点原父级、新父级、修改人、修改时间和备注；
4. 报告预览：公司概览、组织架构图、人才清单、顾问调研总结；
5. 导出开关：可排除 C 类人员、隐藏联系方式、指定报告快照。

### 9.3 快速录入框（文本式建树）

在页面底部提供"快速录入框"（类似记事本）：用户按 Enter 换行，Tab 缩进表示子级，Shift+Tab 提升层级；每行自动解析为一个新节点。

```text
技术部          ← Enter创建（级别0）
  后端组        ← Tab缩进（级别1，父级=技术部）
    张三(架构师) ← Tab缩进（级别2，父级=后端组）
    李四(Java)   ← Enter（同级别2）
  前端组        ← Shift+Tab提升（级别1，父级=技术部）
    王五(React)  ← Tab缩进（级别2）
产品部          ← Shift+Tab提升（级别0）
  赵六(产品经理) ← Tab缩进
```

后端解析逻辑：系统根据行首空格/Tab 数量自动计算 parent_node_id，并解析括号内容"姓名(岗位)"自动填充分类字段。

### 9.4 导出

Excel 内部版：

1. Sheet 1：架构平铺表；
2. Sheet 2：人才明细表。

PDF 客户版：

1. 封面；
2. 公司概览与调研结论；
3. 组织架构图；
4. 人才清单；
5. 调研总结。

---

## 10. BD 助手

### 10.1 定位

BD 助手不是招聘网站爬虫，而是"公开搜索结果整理器 + 顾问核验工作台"。

### 10.2 以人找岗

输入一个候选人简历或候选人档案后：

1. 提取技术方向、业务方向、行业、城市、职级、关键词；
2. 生成多组搜索式，例如"北京 大模型算法 招聘 公司""金融 风控 Java 架构师 职位"；
3. 调用 Bing Web Search API 检索（技术实现见 10.5）；
4. 整理潜在公司、公开职位标题、来源链接、发现时间和匹配原因；
5. 标记为"待 BD 核验"，由用户决定是否带简历沟通。

### 10.3 以岗找公司

输入关键词或 JD 后：

1. 生成"行业 + 公司类型 + 岗位关键词"的搜索计划（含 Bing 搜索式，见 10.5）；
2. 汇集检索到的公司列表；
3. 聚类去重，输出公司、可能的岗位、来源、时间、可信度；
4. 可与已有合作客户/现有岗位比对，标记"非合作客户""已合作但无对应岗位""已有相似岗位"；
5. 输出 BD 报告草稿，不自动发送消息或执行联络。

### 10.4 BD 结果质量规则

每条线索必须记录：搜索任务、关键词、来源 URL、页面标题、发现时间、摘要、匹配依据、人工核验状态。搜索摘要只能说明"发现线索"，不能自动认定企业一定在招聘。

### 10.5 搜索数据源技术实现：SerpApi.com，250次/月；Tavily Search，1000 次 / 月

#### 10.5.1 调用方式

后端（FastAPI）构造简单 GET 请求：

```text
GET https://api.bing.microsoft.com/v7.0/search?q=北京+大模型算法+招聘&count=10
Headers:
  Ocp-Apim-Subscription-Key: {BING_API_KEY}
```

返回 JSON 中直接包含 `webPages` 数组，每个元素：

```json
{
  "name": "XX公司招聘大模型算法工程师",
  "snippet": "岗位职责：负责…任职要求：…",
  "displayUrl": "https://www.example.com/job/123",
  "url": "https://www.example.com/job/123"
}
```

字段映射到 bd_lead 表：

| API 字段               | bd_lead 字段 |
| -------------------- | ---------- |
| `name`               | 页面标题       |
| `snippet`            | 摘要/匹配依据    |
| `url` / `displayUrl` | 来源链接       |
| 请求时间                 | 发现时间       |

#### 10.5.2 公司与岗位提取（无需 LLM）

标题通常形如"XX公司招聘大模型算法工程师""XX科技 急聘 Java 架构师"，直接用轻量正则提取即可：

```text
模式：^(?P<company>[\u4e00-\u9fa5A-Za-z0-9（）()·&]{2,30}?)(?:招聘|招|急聘|诚聘|高薪聘请|社招|校招)?(?P<job>[\u4e00-\u9fa5A-Za-z0-9+/（）()·]+?(?:工程师|架构师|专家|经理|总监|专员|算法|开发|产品|运营|设计师|分析师|顾问|负责人))$
```

提取规则：

1. 公司名：标题中"招聘/急聘/诚聘"前的连续中英文/数字片段（2–30 字符）；
2. 岗位名：关键字（工程师/架构师/专家/经理/总监/专员/算法/开发/产品/运营/设计师/分析师/顾问/负责人）结尾的连续片段；
3. 正则未命中时：降级为整段标题入"待核验"，并标记 `need_llm=true`；只有此时才允许调用 LLM 辅助提取（低频兜底，不进入主链路）；
4. 提取结果与搜索关键词、snippet 一并写入 bd_lead，交由用户核验。

#### 10.5.3 成本与额度

| 项          | 值                                 |
| ---------- | --------------------------------- |
| 免费层        | 每月 1,000 次查询                      |
| 超出后        | 约 $7 / 1,000 次查询                  |
| 单机 BD 顾问用量 | 每天 30 个关键词 ≈ 900 次/月，**免费额度完全够用** |

- 设置页（12.7.3 同级新增"BD 搜索"分组）填写 Bing API Key（加密存储、掩码显示）与"测试连接"；
- 用量控制：搜索计划自动去重、同关键词 7 天内不重复查询；月度用量计数在检测台展示，接近免费额度（>800 次/月）时提醒。

#### 10.5.4 搜索式生成与调用流程

```text
候选人档案 / JD / 关键词
  → 生成多组搜索式（对应 10.2/10.3，如"北京 大模型算法 招聘 公司"）
  → 去重（同关键词 7 天缓存，缓存键 = 关键词 + count + freshness）
  → 调用 Bing Web Search API（串行限速，间隔 ≥ 1s，防 429）
  → 正则提取公司/岗位 → 写入 bd_lead（待核验）
  → 与已有合作客户/岗位比对 → BD 报告草稿
```

- API Key 不可用或超限时：BD 助手降级为"仅生成搜索计划 + 手动打开浏览器搜索"，页面提示原因（错误码规范见 12.5.2），不影响其余模块。

---

## 11. 提示词设计

### 11.1 简历结构化与核心经历提取

```text
角色：你是招聘顾问的结构化简历解析器。

任务：仅依据给定简历原文，返回符合 JSON Schema 的数据。

必须提取：
姓名、电话、身份证号、出生日期、所在地、最高全日制学历、毕业年份、
学校、学校等级、QS2026、最近一段工作行业、任职最长工作行业、
技术方向、业务方向、技能清单、总年限、相关年限、最近两段经历、
任职最长经历、核心项目、核心成果、核心经历摘要。

核心经历摘要格式：
1. 最近职位：公司/职位/时间/行业/关键职责/成果；
2. 最长任职：公司/职位/时间/行业/关键职责/成果；
3. 可迁移能力：技术、业务、管理或客户场景；
4. 待确认项：原文未明确但影响匹配的信息。

规则：
- 只能提取原文明确出现的信息；未知填 null；
- 每一个非空字段附 evidence_text、page_no、confidence；
- 最近行业和最长任职行业必须分别输出；
- 不根据毕业年份推断年龄；年龄仅由明确出生日期计算；
- 技术方向和业务方向优先映射到系统词表，无法映射时保留原词；
- 不编造业绩数字、项目名称、技能或工作时长。
```

简历向量化提取 Prompt（专用于生成向量内容）：

```text
请提取该候选人工作经历和项目经历中，仅与硬技能、行业场景、具体技术栈、
业务指标相关的关键事实。严禁包含：团队协作、沟通能力、抗压能力、领导力、
学习能力强等通用软素质词汇。
输出格式为："动词 + 宾语 + 指标"的紧凑短语列表。
```

### 11.2 JD 解析与核心要求

```text
角色：你是资深招聘顾问，负责把 JD 解析为可检索规则。

任务：根据 JD 原文输出 JSON：
岗位名称、公司、部门、地点、薪资、职级、AI 分类、技术方向、业务方向、
行业、必备条件、加分条件、排除条件、最低相关年限、学历、证书、
关键词、核心要求一句话、原文证据、置信度。

AI 分类规则：
- 核心 AI：职责核心是模型、算法、训练、推理、LLM、数据科学或 AI 产品；
- AI 相关：工作与 AI 产品/平台协作，但不是核心职责；
- 非 AI：其他岗位。

核心要求一句话不超过 40 个汉字，只写真正决定是否合格的 2–4 项条件。
必须/优先/排除必须分别识别，不能把"优先"当成"必须"。
没有证据时填 null 或待人工确认，不得补充猜测。
```

### 11.3 推荐理由

```text
角色：你是招聘顾问助手。

输入：已确认的 JD 结构化字段、候选人档案、相关经历证据。

输出：
1. 3 条以内的推荐理由，每条必须包含"匹配点 + 候选人证据"，不得已主观文字表示，只能客观列举候选人与岗位匹配点；
2. 待确认或明显缺口；
3. 一句结论：强推荐/推荐/可关注/待考虑；
4. 不得声称候选人没有写明的技能、客户、项目或成果。

不要重新给候选人打分；只解释已生成的匹配结果。
```

### 11.4 查询意图解析与提问改写

```text
角色：你是招聘检索的查询解析器。

任务：把用户输入拆分为三类，分别输出：
1. filters：数值/枚举类（年限、学历、地点、QS、状态、电话）→ 结构化过滤；
2. keywords：实体词（技术栈、技能、公司名、岗位名、行业）→ BM25 关键词；
3. query：剩余语义描述 → 向量检索文本。

改写规则：
- 姓名、行业、电话、学历等解析为关键词匹配/过滤条件；
- 其他内容改写为：
  技术方向：{标准词}。业务方向：{标准词}。
  核心职责：{动词+宾语+指标}；{职责2}。
  必需技能：{技能1}、{技能2}、{技能3}。
  加分场景：{行业背景}、{项目类型}。
- 不得在改写中凭空补充用户未提及的条件。
```

---

## 12. 部署、安装与运行环境

### 12.1 数据与存储隔离

1. 每个用户拥有独立、隔离的数据目录，默认路径，可在前端页面修改：
   - Windows：`%USERPROFILE%\recruit_data\`
   - macOS：`~/recruit_data/`
2. 数据目录包含：PostgreSQL 数据、OpenSearch 索引数据、MinIO 原件库、Redis 持久化文件、模型缓存、日志；
3. 禁止使用系统级目录（如 `C:\Program Files`、`/var/`）或跨用户共享目录；
4. 所有数据路径、日志路径、模型缓存路径必须通过配置文件或启动参数指定；提供首次启动配置向导，引导选择数据存放位置并检测目录可写性；
5. 代码中统一使用 `pathlib` 处理路径，兼容 Windows（反斜杠）与 macOS（正斜杠）；配置文件中路径使用正斜杠或由 pathlib 自动转换；
6. 所有数据必须存储在本地物理磁盘，不默认使用网络映射盘、NAS 或云同步文件夹（iCloud、OneDrive 等），防止同步冲突或网络中断导致服务异常。

### 12.2 多操作系统兼容性

1. 开发环境统一：Python 3.11+、Node.js 18+；用 `uv` 管理 Python 包，前端依赖用 `pnpm`/`npm` 管理，避免平台特定二进制包；
2. 容器化优先（强烈推荐）：PostgreSQL、OpenSearch、MinIO、Redis 均使用 Docker Compose 编排，保证 Windows（WSL2 后端）和 macOS 行为一致；提供一键启动脚本（`start.sh` / `start.ps1`）；
3. 原生服务回退方案：若用户不愿使用 Docker，提供独立安装包（各组件原生安装），启动前自动检测端口冲突、Java 环境（OpenSearch 依赖）、系统依赖（Windows 需 Visual C++ Redistributable）；
4. 文件系统差异：注意 Windows 路径长度限制（260 字符），原件库目录尽量扁平；macOS 对文件名大小写敏感，统一使用小写字母 + 下划线命名。（物理存储名使用哈希命名，展示下载名，采用我们设计的<span style="background-color:rgb(235, 235, 235)">{姓名}-{毕业年份}+{学校等级}{学历}-{QS排名}-{技术方向}-{业务方向}</span>）

### 12.3 本地部署与资源适应

1. 自动端口冲突检测：默认端口 PostgreSQL 5432、OpenSearch 9200/9300、Redis 6379、MinIO 9000、API 8000、前端 3000；被占用时自动分配可用端口（+1 递增）并提示用户；所有服务连接信息通过环境变量动态注入；
2. 内存检测：启动时检测可用内存，若 < 8GB 自动启用"低内存模式"（OpenSearch JVM 堆内存降至 512MB，在低内存时，提醒用户，关闭其他应用）；
3. CPU 检测：根据 CPU 核数动态调整后台 Worker 并发数（解析、embedding、索引），默认不超过核数 - 1；
4. GPU 检测：模型走 API 后本机 GPU 非必需；如存在本机 GPU（NVIDIA CUDA 或 macOS MPS）可作可选加速；API 不可用时，提醒用户是否api欠费，或者网络无连接；
5. 磁盘空间监控：定期检查数据目录所在磁盘剩余空间，< 5GB 时发出警告；提供"清理临时文件""归档旧版本""压缩日志"功能入口。

### 12.4 一键安装与启动

1. 打包策略：后端用 PyInstaller 或 Nuitka 打包为单个可执行文件（.exe / .app），前端构建为静态资源内嵌或由内置 HTTP 服务器托管，第三方服务随安装包分发（Docker 镜像或便携版）；
2. 安装流程：
   1. 用户双击安装包 → 解压到用户指定目录；
   2. 首次启动向导：选择数据存储路径（默认 `~/recruit_data`）→ 检测系统资源（内存/CPU/GPU）推荐运行模式 → 校验模型 API 连通性与密钥 → 初始化数据库、创建索引、导入基础词典（QS、同义词）→ 执行预热查询集，确认检索性能达标后进入 READY；向导采集的全部配置写入设置页（12.7），此后可随时在页面内修改路径、API 与各项参数，无需再进向导；
   3. 生成桌面快捷方式（或启动器）；
3. 启动器（Service Manager）：本地服务管理面板（Electron 或 Python Tkinter），依次启动 PostgreSQL、OpenSearch、MinIO、Redis、API；实时显示各服务健康状态（绿/红）；一键停止/重启；崩溃自动重启（最多 3 次）；日志实时滚动显示；
4. 开机自启动（可选）：加入 Windows 注册表启动项或 macOS LaunchAgents。

### 12.5 易用性与用户体验

#### 12.5.1 界面设计

极简三栏布局（左侧导航栏折叠、中央表格、右侧详情抽屉）；全局搜索框置顶，支持自然语言输入（如"找 5 年 Java 经验、985 硕、看金融机会"）；核心操作（匹配、导出、推荐）使用大按钮，复杂参数隐藏于高级设置；所有异步任务（导入、解析、导出）在界面左上角展示进度条，可点击查看详情。

#### 12.5.2 错误友好提示

禁止直接抛出堆栈异常；统一封装为中文友好提示 + 错误码，例如 `E1001` 检索引擎未启动、`E2003` 简历解析超时；错误详情写入日志供排查。

#### 12.5.3 内置帮助与引导

首次进入提供引导气泡（Tour Guide）；侧边栏帮助中心（常见问题、快捷键、视频教程链接，本地 Markdown/HTML）；每页放置"？"图标弹出功能说明。

#### 12.5.4 诊断包导出

设置页提供"导出诊断包"按钮，一键打包：所有组件日志（最近 7 天）、脱敏后的配置文件、系统环境信息（OS/内存/CPU/GPU/磁盘）、数据库 Schema 版本、索引 mapping 版本。

#### 12.5.5 全局快捷键

| 快捷键              | 功能                                                  |
| ---------------- | --------------------------------------------------- |
| Cmd/Ctrl + K     | 聚焦全局搜索框（最重要）                                        |
| Cmd/Ctrl + 1–7   | 快速切换页面（1=人才库，2=JD 管理，3=匹配，4=看板，5=Mapping，6=BD，7=设置） |
| Cmd/Ctrl + Enter | 提交当前表单                                              |
| Esc              | 关闭弹窗/抽屉                                             |
| Cmd/Ctrl + S     | 保存当前内容                                              |
| Space            | 在表格中选中行并预览简历                                        |

快捷键在帮助中心可查，支持自定义（设置页"快捷键"分组，见 12.7.1）。

#### 12.5.6 操作撤销（correction_logs）

**问题**：用户可能误操作（如误点"推进"、误改候选人字段），缺乏撤销能力。

**设计**：所有手工修改（字段编辑、流程推进、状态变更、Mapping 调整、BD 核验等）写入 `correction_logs` 表（见 4.2），支持回滚：

- 记录内容：table_name、row_id、字段名、旧值、新值、修改人、修改时间；
- 每个可编辑页面的最近一次修改旁提供"撤销"按钮（Ctrl/Cmd + Z 也可触发）；
- 回滚动作：将当前值恢复为旧值，并新增一条"回滚"日志（回滚本身也留痕）；
- 冲突处理：若目标行已被后续修改覆盖，撤销时提示"该记录已被再次修改，撤销将覆盖最新值"，需二次确认；
- 回滚范围：候选人字段、JD 字段、case 流程状态、Mapping 节点、BD 线索核验状态均支持；软删除/恢复动作也写入日志（配合 4.6 回收站）。

### 12.6 数据安全与隐私

说明：本系统不做多租户、角色权限等产品级隐私能力；以下为**单机本地部署的安全加固**要求，两者不冲突。

1. 敏感字段加密：身份证号、手机号在数据库中加密存储（AES-256-GCM，密钥由用户首次启动时生成并存储在本地）；姓名、地址、工作经历等字段不加密，便于检索；日志中打印敏感字段自动脱敏（如手机号 `138****1234`）；
2. 本地访问限制：所有服务默认绑定 `127.0.0.1`/`localhost`，禁止绑定 `0.0.0.0` 防止局域网其他设备访问；如需对外开放（如调试）通过配置文件显式开启并提示安全风险；
3. 原件库安全：原件存储路径使用两级 SHA-256 哈希分片命名（`blobs/{h0}/{h1}/{sha256}.pdf`），即使文件被直接访问也难以关联到具体候选人；原件库目录默认仅当前用户可读写（默认 `~/recruit_data/blobs/` 权限 700）；
4. 备份与恢复：提供手动备份（数据库 + 原件库 + 配置到用户指定目录如外部硬盘）；提供自动备份（每日凌晨，保留最近 7 天，自动清理旧备份）；恢复时用户选择备份文件自动还原。

### 12.7 设置页面（Settings）

定位：系统所有可运行参数（数据存放路径、各模型 API、邮箱、提醒、备份等）统一收敛到一个可视化设置页面，使用者无需手改配置文件即可完成日常配置与维护。配置文件是底层兜底，设置页是唯一推荐入口；两者以"最后一次保存"为准，页面加载时以配置文件为准回显。

#### 12.7.1 页面入口与布局

- 左侧导航栏提供"设置"入口，右侧内容按分组以分区卡片/页签展示：数据与存储、模型 API、邮箱、提醒与通知、备份与维护、系统与升级、快捷键（可自定义 12.5.5 快捷键）；
- 所有修改先"暂存"，确认后点击"保存并应用"统一生效；应用前逐项校验，未通过项标红并给出错误码（沿用 12.5 的错误码规范）；
- 每次保存记录变更日志（修改项、旧值、新值、修改时间），可在页面内查看，便于回退。

#### 12.7.2 数据与存储

1. 数据根目录（DATA_DIR）：展示当前路径、所在磁盘剩余空间与数据占用；修改时校验新目录可写、空间充足、不在系统目录（`C:\Program Files`、`/var` 等）；
2. 原件库目录、备份目录、日志目录可分别指定，默认落在数据根目录下；原件库内部固定为哈希分片结构（`blobs/{h0}/{h1}/{sha256}.pdf`），不随配置改变目录层级；
3. 修改数据路径提供两种模式：
   - **迁移现有数据**：后台执行"暂停写入 → 复制/移动（数据库、原件库、索引、Redis 持久化）→ 逐项校验 → 切换配置 → 重启服务"，迁移期间界面显示进度，失败自动回滚到旧路径；
   - **仅新数据使用新路径**：不移动旧数据，仅后续写入走新目录（用于数据已迁移完成的场景）；
4. 端口配置（高级，默认自动检测）：PostgreSQL / OpenSearch / Redis / MinIO / API / 前端各端口，冲突时沿用 12.3 的 +1 自动分配；
5. 资源模式：自动 / 低内存模式开关（对应 12.3.2），可手动覆盖。

#### 12.7.3 模型 API 配置（每个 API 独立填写 / 更换 / 测试）

| API          | 可配置项                                                     | 测试连接                | 更换提示                                                    |
| ------------ | -------------------------------------------------------- | ------------------- | ------------------------------------------------------- |
| LLM（解析/理由生成） | 模型选择（DeepSeek V4 Flash / V4 PRO）、Base URL、API Key、超时、并发数 | 发送最小测试请求校验连通性、鉴权与额度 | 提示"更换后新解析结果与旧结果并存，历史记录不受影响"                             |
| Embedding    | 模型（默认 BGE-M3）、Base URL、API Key、批量大小                      | 校验连通性、鉴权与向量维度       | **提示"检索版本变更，需全量重新向量化并重新预热"**（对应 7.2 检索版本规则），确认后进入后台重算任务 |
| Reranker     | 模型（默认 bge-reranker-v2-m3）、Base URL、API Key、batch size    | 校验连通性、鉴权            | 提示"检索版本变更，需重新跑回归集"后生效                                   |
| BD 搜索（Bing）  | API Key、每请求 count（默认 10）、同关键词缓存天数（默认 7）                  | 发送一次最小查询校验鉴权与剩余额度   | 提示"更换后新线索检索生效，历史线索保留"                                   |

规则：

1. 每个 API 独立"启用/停用"开关；停用后系统按第 13 章降级矩阵自动退化（如 Embedding 停用 → BM25 + 结构化过滤），页面显著标注当前降级状态；
2. API Key 使用 AES-256-GCM 加密存储（密钥随系统首启生成，与 12.6 敏感字段同机制），界面仅显示掩码（如 `sk-****xxxx`），不提供明文回显；导出诊断包时自动脱敏；
3. "测试连接"在保存前即可执行，测试通过才允许保存；测试请求不计入检索延迟预算；
4. 模型服务状态实时展示：各 API 最近一次探测结果（正常 / 失败 / 未配置）、失败原因提示（如"API 欠费""网络无连接"，对应 12.3.4）；
5. 更换 Embedding 模型会触发全量重算：确认弹窗展示预估耗时与"将暂停新简历向量化直到完成"的说明，可随时在任务中心查看进度、暂停或取消。

#### 12.7.4 邮箱（Agent 邮箱）

1. IMAP 服务器、端口、账号、授权码（加密存储）、SSL 开关；
2. "测试连接"按钮校验登录与收件箱可读；
3. 白名单域名维护（默认 boss.com、liepin.com，增删即时生效）；
4. 显示监听状态：实时监听中 / 轮询中（间隔）/ 断线重连中。

#### 12.7.5 提醒与通知

1. 早 9:00 追反馈提醒：开关 + 时间设置 + 模板编辑（AI 按追反馈格式生成正文，对应 8.3）；
2. 晚 17:00 未处理提醒：开关 + 时间设置；
3. 提醒发送邮箱账号（默认使用 12.7.4 的邮箱账号）。

#### 12.7.6 备份与维护

1. 自动备份：开关、执行时间（默认每日凌晨）、保留天数（默认 7 天）、备份目标目录；
2. "立即备份"按钮，可查看最近备份时间、大小与状态；
3. 磁盘空间管理入口：清理临时文件、归档旧版本、压缩日志（对应 12.3.5）；
4. 回收站清理策略：到期自动清理 / 到期仅提醒（默认"仅提醒"，对应 4.6.3）；match_result 快照保留时长（默认 6 个月，对应 13.1.1）。

#### 12.7.7 系统与升级

1. "检查更新"按钮（对应第 14 章）；展示当前应用版本、数据库 Schema 版本、索引 mapping 版本、各模型版本；
2. "导出诊断包"按钮（对应 12.5.4）；
3. 系统信息摘要：OS 版本、内存/CPU/磁盘、各服务健康状态；
4. "健康检测台"入口开关（默认折叠可打开，对应 13.3）。

#### 12.7.8 生效规则

| 修改类型                                             | 生效方式                              |
| ------------------------------------------------ | --------------------------------- |
| 提醒时间、白名单域名、开关类、回收站清理策略、match_result 保留时长、BD 搜索配置 | 立即生效                              |
| 端口、数据路径、资源模式                                     | 提示"将在服务重启后生效"，提供"立即重启"按钮          |
| 更换 LLM / Embedding / Reranker 模型                 | 保存后按 12.7.3 规则触发重算或回归，期间服务按降级矩阵运行 |

---

## 13. 异常、重试和可维护性

| 场景                | 处理                                                         |
| ----------------- | ---------------------------------------------------------- |
| 文件格式不支持           | 标记 REJECTED，展示原因，不写入搜索                                     |
| 加密/损坏文件           | 标记 NEEDS_REVIEW，可手动补传                                      |
| OCR 失败            | 自动重试；超过阈值进入 DEAD_LETTER                                    |
| LLM 解析失败          | 保留原文和文本提取结果，支持重新解析                                         |
| 向量写入失败            | revision 保留 PARSED 状态，Worker 指数退避重试（最多 5 次）；未 READY 前不参与检索 |
| Excel 导出失败        | 保留 match_run 快照，支持重新生成                                     |
| 邮件监听断线            | 重连后按 UIDVALIDITY/UID 继续拉取，不漏不重                             |
| Embedding API 不可用 | 已预计算向量的 JD 可继续走向量检索；自由文本查询退化为 BM25 + 结构化过滤                 |
| Reranker API 不可用  | 降级为 Hybrid 未重排模式，仅在该模式已独立通过评测时开放，并在页面标注"未重排"               |
| 全部模型 API 不可用      | 仅开放 BM25 + 硬过滤；保证硬条件正确与响应速度，不承诺完整排序指标                      |
| Bing API 不可用/超限   | BD 助手降级为"仅生成搜索计划 + 手动浏览器搜索"，页面提示原因与剩余额度；不影响其他模块（见 10.5.5）  |

日志与监控：

- 日志格式统一为 JSON（时间、级别、模块、消息、trace_id）；按天滚动，保留 30 天，自动压缩归档；
- 提供健康检查端点 `/health`，返回各组件状态，启动器定时调用并更新 UI。

### 13.1 性能与资源维护

#### 13.1.1 OpenSearch 索引膨胀

**问题**：简历不断入库与更新后，OpenSearch 索引会持续产生新 segment，旧数据段不会自动清理，导致磁盘占用只增不减、检索性能逐渐下降（即使简历总数未增加）。

**维护策略**：

1. 每日凌晨执行 `POST /_forcemerge?max_num_segments=1`，合并索引段、释放磁盘空间（放在低峰期，见 13.2 每日任务）;
2. 监控索引大小：每日巡检记录索引占用；超过阈值（如 candidate_profile 索引 > 4GB）在检测台/看板发出警告；
3. 定期清理 `match_result` 过期匹配快照：保留最近 6 个月，更早的快照转为归档（可在设置页配置保留时长），清理后触发索引段合并；
4. 索引一致性检查每日执行：比对 PostgreSQL 中 READY 的 revision 与索引文档数，差异过大触发重建任务（走 batch 队列）。

#### 13.1.2 PostgreSQL 事务日志（WAL）膨胀

**问题**：PostgreSQL 的 WAL 日志会不断累积，若不归档或清理，最终占满磁盘。

**维护策略**：

1. 在 `postgresql.conf` 中配置 `wal_keep_size = 1GB`，限制 WAL 保留大小；
2. 每日自动执行 `VACUUM ANALYZE`：清理死元组、更新统计信息（纳入 13.2 每日任务）；
3. 每月执行 `VACUUM FULL` 回收磁盘空间（会锁表，安排在低峰期，如周末凌晨）；
4. 若启用归档，配置 `archive_mode=on` + 定期清理过期归档日志。

#### 13.1.3 日志文件无限增长

**问题**：运行数月后 `logs/` 目录可能达到几十 GB。

**维护策略**：

1. 日志按天滚动：`logs/app-2026-08-28.log`，当天单独文件；
2. 自动清理 30 天前的日志（压缩归档后删除，保留压缩包可选）；
3. 日志中不打印敏感信息：手机号、身份证号等自动脱敏（`138****1234`），敏感字段不落日志；
4. 日志目录纳入磁盘空间监控（<5GB 告警，见 12.3.5）。

### 13.2 每日自维护任务

每日执行：数据库备份、原件库备份、索引一致性检查、失败任务重试、词表和 QS2026 映射校验、OpenSearch 索引段合并优化（forcemerge）、PostgreSQL 自动 VACUUM ANALYZE、清理过期临时文件和缓存、回收站到期对象处理（见 4.6.3）、match_result 过期快照归档检查。每月低峰期额外执行：PostgreSQL VACUUM FULL、WAL 归档清理、日志压缩归档。

### 13.3 健康检查与启动就绪提示

1. 启动就绪：所有功能（PostgreSQL、OpenSearch、Redis、MinIO、API、前端、模型 API 连通性）正常启动并检测通过后，页面弹出/常驻提示"所有功能已就绪，可以开始使用"，未就绪前展示各组件状态与失败原因（对应 3.2 READY 门槛）；
2. 检测台：页面底部提供可选的"检测台"面板（默认折叠，可打开），实时显示：
   - 前端（页面可交互性）
   - 后端 API（`/health` 及各关键接口探活）
   - 模型 API（LLM / Embedding / Reranker 连通性与最近一次探测结果）
   - 数据库（PostgreSQL 连接、WAL 大小、表空间）
   - 检索引擎（OpenSearch 连接、索引大小、分片状态）
   - 队列（Redis 连接、各队列积压量、Worker 存活数）
   - 原件库（磁盘剩余空间、blob 引用一致性）
3. 任一组件异常时，检测台红色标注 + 中文错误提示（错误码规范见 12.5.2），并给出处理建议（如"检索引擎未启动，请检查端口是否被占用 E1001"）；
4. 检测台数据来源为 `/health` 与各组件探活接口，页面每 30 秒自动刷新，也可手动刷新。

---

## 14. 版本升级与持续维护

1. 升级策略：增量升级包（只打包变更文件：Python 代码、前端静态资源、数据库迁移脚本），用户下载解压覆盖；
2. 数据库迁移：使用 Alembic 管理 Schema 变更，升级前自动备份数据库，迁移失败自动回滚；
3. 索引迁移：若 OpenSearch mapping 变更，创建新索引并执行 reindex，完成后原子切换 alias；
4. 向前兼容：新版本必须能读取旧版本数据格式（原件库、数据库、索引）；不兼容变更需提供迁移工具和文档；
5. 检查更新：应用启动时或设置页提供"检查更新"按钮（内部更新服务器或 GitHub Releases），提示下载增量包并引导安装；不强制更新，允许跳过。

---

## 15. 测试与质量保障

### 15.1 多环境测试矩阵

- 操作系统：Windows 10/11、macOS 13/14/15；
- 硬件配置：低配（8GB RAM、2 核 CPU、无 GPU）、中配（16GB、4 核、核显）、高配（32GB+、8 核+、NVIDIA GPU）；
- 测试场景：首次安装、升级安装、数据迁移、崩溃恢复、磁盘满、端口冲突。

### 15.2 自动化测试

- 单元测试：核心数据模型、工具函数、解析逻辑（pytest）；
- 集成测试：API 接口测试（httpx）、数据库操作、OpenSearch 查询；
- 端到端测试：Playwright 模拟用户操作（上传简历、搜索、匹配、导出）。

### 15.3 性能回归

- 每次发版前在固定基线硬件上运行压测套件，确保 P95/P99 不劣化；
- 构建至少 800 条标注查询集（含中英混合、低频技能、强过滤各不少于 100 条），双人盲标、分歧仲裁；
- 每次替换解析模型、embedding、重排模型、同义词词典或权重，都必须跑同一回归集。

### 15.4 用户验收测试（UAT）

- 在真实业务人员机器上进行 Beta 测试，收集反馈并修复；
- 建立问题反馈渠道（内部群、Issue Tracker）。

---

## 16. 验收标准

### 16.1 功能验收

| 功能      | 验收条件                                                             |
| ------- | ---------------------------------------------------------------- |
| 批量上传    | 文件夹上传 1,000 份简历可展示逐文件状态、可重试、可查询失败原因                              |
| 去重      | 同文件不重复写原件；同身份证自动关联；同手机号提示人工确认                                    |
| 版本      | 上传新简历后当前版切换、旧版可查看，最近历史版保留                                        |
| 邮箱      | 重启后不漏邮件附件、不重复入库                                                  |
| 命名      | 自动生成毕业年、学校等级、学历、QS2026、技术/业务方向命名                                 |
| 人才表     | 能按全部指定字段筛选、查看、编辑、下载 Excel                                        |
| JD      | 文本/Word/Excel 均能入库、解析、关闭和查看历史版本                                  |
| 匹配      | 关键词、单 JD、批量 JD 均能返回候选人并导出对应 Sheet                                |
| 看板      | 推荐量和每阶段通过率可下钻至候选人与岗位明细                                           |
| Mapping | 可拖拽组织树、快速录入框建树、保存快照、导出 Excel/PDF                                 |
| BD      | 能输出带来源链接和人工核验状态的公司线索报告；Bing API 调用成功、正则提取公司/岗位、免费额度内运行           |
| 安装部署    | Windows/macOS 均可一键安装、一键启动、端口冲突自动处理                               |
| 设置页面    | 可修改数据存放路径（含数据迁移）、逐项填写/更换/测试各模型 API 并加密存储密钥、配置邮箱/提醒/备份，保存后按规则生效   |
| 安全      | 敏感字段加密存储、日志脱敏、服务默认仅本机可访问                                         |
| 提醒      | 早 9 点追反馈、晚 5 点未处理提醒按计划触发                                         |
| 异步任务    | 上传/匹配/导出/重算均立即返回任务 ID；任务中心可见进度、失败原因与错误码；进程重启后任务不丢、可恢复；批量任务可暂停/继续 |
| 软删除/回收站 | 误删候选人/岗位后可在回收站恢复；30 天到期按设置策略提示或自动清理；物理清理需二次确认                    |
| 撤销      | 误改字段/误推进流程可撤销回滚，correction_logs 记录留痕，冲突时二次确认                     |
| 快捷键     | Cmd/Ctrl+K 聚焦搜索、1–7 切页、Enter 提交、Esc 关闭、S 保存、Space 预览生效           |
| 健康检测台   | 启动就绪提示展示；检测台可查看前端/后端/API/数据库/检索/队列/原件库状态，异常标红并给出错误码              |

### 16.2 检索与准确度验收

1. 以 10 万名候选人的真实或脱敏样本完成压测；
2. READY 门槛要求模型 API 就绪、索引 alias 已切换、全预热集完成且健康检查成功；独立重启后的第一条真实查询端到端不高于 2.5 秒；
3. 在 10 并发、5 QPS、同时有正常后台任务的代表性压测中，首屏 P95 不高于 1.5 秒、P99 不高于 2.5 秒；
4. 硬条件错误为 0，例如已确认的地点、最低相关年限、岗位关闭状态；字段未知/低置信度必须显示待确认，不能误判为不满足；
5. 构建至少 800 条标注查询，采用 0–3 级相关性，并保留中英混合、低频技能、强过滤各不少于 100 条；双人盲标、分歧仲裁；
6. Recall@300 不低于 98%，nDCG@10 不低于 0.85；
7. Top 20 覆盖人工 shortlist 不低于 95%，并与纯 BM25 基线作非劣性比较；
8. 每次替换解析模型、embedding、重排模型、同义词词典或权重，都必须跑同一回归集。

---

## 17. 分期实施建议

| 阶段             | 交付范围                                                                                                          | 上线门槛                 |
| -------------- | ------------------------------------------------------------------------------------------------------------- | -------------------- |
| Phase 0：检索底座   | 简历上传、版本、解析、人才表、标准词表、搜索索引、性能评测、首次预热、配置管理（路径/端口/资源自适应）、Docker Compose、Windows/macOS 启动脚本、模型 API 校验 + 预热机制、诊断包导出 | 10 万简历检索首屏 P95 1.5 秒 |
| Phase 1：招聘闭环   | JD 管理、单/批量匹配、Excel 导出、候选人-JD 流程、基础看板、启动器（Service Manager）、软删除与回收站、操作撤销、全局快捷键                                  | 顾问能完成导入到推荐的完整闭环      |
| Phase 2：自动化与交付 | IMAP 邮箱、自动反向匹配、Mapping 树、Excel/PDF 报告、定时提醒、健康检测台、性能维护任务（forcemerge/VACUUM/日志滚动）                               | 报告数据可回溯至快照           |
| Phase 3：BD 助手  | 搜索计划、Bing Web Search API 接入、公司/岗位正则提取、线索收集、去重、客户比对、BD 报告                                                      | 每条线索均有来源与核验状态        |
| 交付前            | 安装包制作、多环境测试矩阵、用户手册 + 内置帮助、升级路径测试、代码与配置脱敏检查                                                                    | 全部通过                 |

Phase 0 是所有后续能力的前置条件。没有稳定的候选人主档案、词表、索引与评测，匹配、看板、Mapping 和 BD 都会建立在不可控数据上。

---

## 18. 原始需求覆盖核对

| 原始模块                                 | 本方案对应章节            |
| ------------------------------------ | ------------------ |
| 简历管理、原件、三层存储、人才表、自动匹配                | 第 4、5 章            |
| JD 解析、列表、详情、关闭岗位                     | 第 6 章              |
| RAG/检索、Excel 匹配结果、推荐理由、意图解析与提问改写     | 第 2.3、7、11 章       |
| 推荐统计、面试漏斗、图表、手动标记驱动、定时提醒             | 第 8 章              |
| 公司/部门/人员 Mapping、树编辑、快速录入框、PDF/Excel | 第 9 章              |
| 以人找岗、以岗找公司、Bing API 搜索与正则提取          | 第 10 章             |
| 10 万简历秒级准确检索与首次使用预热                  | 第 3、7、16 章         |
| 部署安装、跨平台、资源自适应、安全、日志、升级、测试           | 第 12、13、14、15 章    |
| 设置页面（数据路径、API 配置、邮箱、提醒、备份、系统）        | 第 12.7 章           |
| 异步任务队列（Redis RQ、状态持久化、前端轮询、任务中心）     | 第 2.4 章            |
| 原件存储哈希分片 + 月份逻辑分区                    | 第 5.3.1、12.6 章     |
| 软删除与回收站（30 天恢复/自动清理）                 | 第 4.6、12.7.6 章     |
| 性能与资源维护（索引/WAL/日志）与健康检测台             | 第 13.1、13.2、13.3 章 |
| 全局快捷键与操作撤销（correction_logs）          | 第 12.5.5、12.5.6 章  |

---

## 19. 最终决策

本方案采用"本地/私有单人工作台 + PostgreSQL 事实库 + 统一混合搜索引擎 + 异步解析索引"的架构，Windows / macOS 本机独立安装使用，所有模型（DeepSeek V4、BGE-M3、bge-reranker-v2-m3）统一通过 API 调用、不本地部署。

关键承诺不是"任意未处理数据都瞬时可搜"，而是：

1. 初始批量导入完成后，搜索模型、索引和缓存均已预热；
2. 首条真实检索不发生模型加载、索引构建或服务冷启动；
3. 10 万份当前有效简历的候选人检索首屏稳定在秒级；
4. 硬筛、精确关键词、语义召回、重排和推荐理由分层执行，速度与准确度都可测试、可回归；
5. 部署与运维以单机易用为前提：独立数据目录、资源自适应、一键安装与启动、敏感字段加密、定期自维护，保证单人顾问可长期稳定使用。

---

## 附录 A：关键配置项清单

| 配置项                                             | 默认值                  | 说明                                        |
| ----------------------------------------------- | -------------------- | ----------------------------------------- |
| `DATA_DIR`                                      | `~/recruit_data`     | 所有数据存储根目录                                 |
| `POSTGRES_PORT`                                 | `5432`               | PostgreSQL 端口                             |
| `OPENSEARCH_PORT`                               | `9200`               | OpenSearch REST API 端口                    |
| `REDIS_PORT`                                    | `6379`               | Redis 端口                                  |
| `MINIO_PORT`                                    | `9000`               | MinIO API 端口                              |
| `API_PORT`                                      | `8000`               | 后端 API 端口                                 |
| `FRONTEND_PORT`                                 | `3000`               | 前端开发服务器端口                                 |
| `OPEN_SEARCH_JVM_HEAP`                          | `2g`                 | OpenSearch 堆内存（低内存模式 512m）                |
| `LLM_MODEL`                                     | `deepseek-v4-flash`  | 生成模型（可选 deepseek-v4-pro）                  |
| `LLM_API_BASE` / `LLM_API_KEY`                  | —                    | LLM 服务地址与密钥（设置页填写，加密存储）                   |
| `EMBEDDING_MODEL`                               | `BGE-M3`             | Embedding 模型（API）                         |
| `EMBEDDING_API_BASE` / `EMBEDDING_API_KEY`      | —                    | Embedding 服务地址与密钥（设置页填写，加密存储）             |
| `RERANKER_MODEL`                                | `bge-reranker-v2-m3` | 重排模型（API）                                 |
| `RERANKER_API_BASE` / `RERANKER_API_KEY`        | —                    | 重排服务地址与密钥（设置页填写，加密存储）                     |
| `IMAP_HOST` / `IMAP_ACCOUNT` / `IMAP_AUTH_CODE` | —                    | 邮箱监听配置（设置页填写，授权码加密存储）                     |
| `BING_API_KEY`                                  | —                    | Bing Web Search API 密钥（设置页填写，加密存储，见 10.5） |
| `BING_COUNT`                                    | `10`                 | 单次 BD 查询返回条数（默认 10）                       |
| `BING_CACHE_DAYS`                               | `7`                  | 同关键词 BD 查询缓存天数（防重复计费）                     |
| `MAX_WORKERS`                                   | `CPU 核数 - 1`         | 后台 Worker 并发数                             |
| `LOG_LEVEL`                                     | `INFO`               | 日志级别                                      |
| `BACKUP_RETENTION_DAYS`                         | `7`                  | 自动备份保留天数                                  |
| `RECYCLE_BIN_RETENTION_DAYS`                    | `30`                 | 回收站保留天数（到期后按 4.6.3 策略处理）                  |
| `RECYCLE_AUTO_PURGE`                            | `false`              | 回收站到期自动清理开关（默认仅提醒）                        |
| `MATCH_RESULT_RETENTION_DAYS`                   | `180`                | match_result 快照保留天数（默认 6 个月，过期归档）         |
| `WAL_KEEP_SIZE`                                 | `1GB`                | PostgreSQL WAL 保留大小（13.1.2）               |
| `LOG_RETENTION_DAYS`                            | `30`                 | 日志保留天数（按天滚动，过期清理）                         |
| `CACHE_TTL`                                     | `3600`               | 查询缓存 TTL（秒）                               |
| `MORNING_REMINDER_TIME`                         | `09:00`              | 早间追反馈提醒时间                                 |
| `EVENING_REMINDER_TIME`                         | `17:00`              | 傍晚未处理提醒时间                                 |

> 说明：除 `LOG_LEVEL` 等少量调试项外，上表全部配置均可通过**设置页（第 12.7 章）**&#x4FEE;改并校验，配置文件仅作为底层存储与兜底；包含密钥的配置项在设置页仅显示掩码。

## 附录 B：开发检查清单

**Phase 0（检索底座）**：

- [ ] 配置管理（路径、端口、资源自适应）完成
- [ ] PostgreSQL + OpenSearch + MinIO + Redis Docker Compose 编排完成
- [ ] Windows/macOS 启动脚本（含端口检测）完成
- [ ] 模型 API 连通性校验 + 预热机制完成
- [ ] 简历上传、解析、索引端到端流程完成
- [ ] 原件库哈希分片存储（两级分片、去重、ingest_month 逻辑分区）完成
- [ ] 异步任务队列（RQ + task 表持久化 + 前端轮询 + 任务中心）完成
- [ ] 人才库搜索（含硬过滤、BM25、ANN、RRF）完成
- [ ] 10 万数据压测达标
- [ ] 诊断包导出功能完成

**Phase 1（招聘闭环）**：

- [ ] JD 上传、解析、版本管理完成
- [ ] 单 JD / 批量 JD 匹配 + Excel 导出完成
- [ ] 候选人-JD 流程（Case + Stage Event）完成
- [ ] 基础看板（推荐量、漏斗）完成
- [ ] 启动器（Service Manager）完成
- [ ] 设置页面完成（数据路径与迁移、各模型 API 填写/测试/更换、邮箱、提醒、备份、系统信息）
- [ ] 软删除与回收站（30 天恢复、到期清理策略）完成
- [ ] 操作撤销（correction_logs 记录与回滚）完成
- [ ] 全局快捷键（搜索聚焦、切页、保存、预览等）完成

**Phase 2（自动化与交付）**：

- [ ] IMAP 邮箱监听 + 被动入库完成
- [ ] 自动反向匹配完成
- [ ] Mapping 树 + 快速录入框 + Excel/PDF 报告完成
- [ ] 定时提醒（追反馈 / 未处理）完成
- [ ] 健康检测台（启动就绪提示 + 组件状态面板）完成
- [ ] 性能维护任务（forcemerge、VACUUM ANALYZE/FULL、日志滚动清理、match_result 归档）完成

**Phase 3（BD 助手）**：

- [ ] 以人找岗 / 以岗找公司搜索计划完成
- [ ] Bing Web Search API 接入（密钥配置、测试连接、限额提醒）完成
- [ ] 公司/岗位正则提取（LLM 兜底）完成
- [ ] 线索收集 + 去重 + 核验完成
- [ ] BD 报告导出完成

**交付前**：

- [ ] 安装包（含 Docker 镜像或原生依赖）制作完成
- [ ] 多环境测试矩阵全部通过
- [ ] 用户手册 + 内置帮助完成
- [ ] 升级路径测试完成
- [ ] 代码和配置脱敏检查通过

---

*本文档由三份源文件整合而成：原设计方案（16 章骨架）、设计补充（检索意图解析/结构化模板/看板/Mapping/模型选型/定时提醒）与部署运维要求（跨平台/安装/安全/升级/测试）。版本 1.1，后续迭代时请在每章评审对照检查。*
